apiVersion: devloop/v1
kind: Config
build:
  artifacts:
    - image: my-image
      context: my-image
manifests:
  rawYaml:
  - my-manifests-*
resourceSelector:
  allow:
    - groupKind: "CustomDeployment.devloop.dev"
      image: [".*"]
      labels: [".*"]