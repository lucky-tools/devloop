apiVersion: devloop/v1
kind: Config
build:
  artifacts:
  - image: devloop-example
test:
  - image: devloop-example
    structureTests:
      - ./test/*
manifests:
  rawYaml:
    - k8s-*
profiles:
  - name: test
    test:
      - image: devloop-example
        structureTests:
          - ./test/profile_structure_test.yaml
