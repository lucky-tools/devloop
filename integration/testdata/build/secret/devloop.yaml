apiVersion: devloop/v1
kind: Config
build:
  local:
    cli:
      useBuildKit: true
    push: false
  artifacts:
  - image: secret
    docker:
      secret:
        id: mysecret
        src: mysecret.txt
