# Integration Tests for `devloop debug`

These are a set of test projects for `devloop debug`.  There are two
configurations:

  - `devloop.yaml` configures docker- and jib-based builders
  - `devloop-bp.yaml` configures buildpacks-based builders

The test projects endeavour to support both docker or jib, and buildpacks.
