apiVersion: "run.googleapis.com/v1"
kind: WorkerPool
metadata:
  name: devloop-test-wp
  annotations:
    run.googleapis.com/launch-stage: BETA
spec:
  template:
    spec:
      containers:
      - image: us-docker.pkg.dev/cloudrun/container/worker-pool
