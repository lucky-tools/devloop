apiVersion: devloop/v1
kind: Config
build:
  tagPolicy:
    sha256: {}
  local:
    push: false
    cli: {}
  artifacts:
  - image: gcr.io/k8s-devloop/devloop
    context: base
    docker:
      dockerfile: Dockerfile
      noCache: true
      buildArgs:
        IMAGE_REPO: '{{.IMAGE_REPO}}'
        IMAGE_NAME: '{{.IMAGE_NAME}}'
        IMAGE_TAG: '{{.IMAGE_TAG}}'
  - image: child
    requires:
    - image: gcr.io/k8s-devloop/devloop
      alias: BASE
    context: child
    docker:
      dockerfile: Dockerfile
      noCache: true
deploy:
  docker:
    images: [child]
