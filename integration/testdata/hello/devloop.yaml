apiVersion: devloop/v1
kind: Config
build:
  artifacts:
  - image: devloop-hello

profiles:
  - name: pod
    manifests:
      rawYaml:
      - k8s/pod.yaml
  - name: deployment
    manifests:
      rawYaml:
      - k8s/deployment.yaml