apiVersion: devloop/v1
kind: Config
build:
  artifacts:
    - image: devloop-example
      kaniko:
        target: runner
        cache: {}
  cluster:
    pullSecretName: e2esecret
    namespace: default
manifests:
  rawYaml:
  - k8s-*
