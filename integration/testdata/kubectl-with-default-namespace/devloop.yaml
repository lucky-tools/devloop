apiVersion: devloop/v1
kind: Config

build:  
  artifacts:
  - image: devloop-example
    context: .
    docker:
      dockerfile: Dockerfile

manifests:
  rawYaml:
  - k8s-*

deploy:
  kubectl:
    defaultNamespace: "{{.ENV1}}"